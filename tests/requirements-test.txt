# Test dependencies for Claude Cadence
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
pytest-timeout>=2.1.0
pytest-xdist>=3.3.0  # For parallel test execution
pytest-env>=0.8.0   # For environment variables
fakeredis>=2.18.0   # For Redis mocking if needed
freezegun>=1.2.0    # For time mocking
responses>=0.23.0   # For HTTP mocking
